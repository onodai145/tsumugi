# モバイルUI E2E Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** デスクトップ WebKitGTK 上で `uiMode=mobile` + 小ビューポート + safe-area 疑似注入により、モバイルUIのレイアウト崩れを自動検出する E2E を追加する（Issue #259）。

**Architecture:** 本番CSSの `env(safe-area-inset-*)` を `:root` の CSS 変数 `--safe-*` 経由に変更し、E2E から上書き可能にする。E2E は既存の wdio + tauri-driver + テスト用 Misskey 基盤を流用し、`specs-mobile/` と専用 config `wdio.mobile.conf.ts` を追加する。検証は `getBoundingClientRect()` と注入 inset の比較で行う。

**Tech Stack:** Svelte 5 + Tailwind (arbitrary values), WebdriverIO 9 + tauri-driver, Mocha, TypeScript。

設計: `docs/superpowers/specs/2026-09-26-mobile-e2e-design.md`

## Global Constraints

- ブランチは `docs/259-mobile-e2e-design` を継続使用してよい（mainへは直接コミットしない）。
- コミットメッセージは件名のみ・本文なし（Co-Authored-By トレーラーのみ別途付与）。
- 実機の見た目・挙動は変えない（`var(--safe-*)` は実機では `env()` と同値）。
- ウィンドウサイズ/`uiMode` の変更は E2E の一時プロセス内のみ（`run-app.sh` が毎回 `mktemp` した XDG 領域を使うため本番設定に影響しない）。
- 新規 UI に one-off な値を発明しない（`docs/design/style-guide.md` 準拠）。
- `cargo tauri dev` / 実機での目視確認は不要。E2E 実行は Xvfb 越し（`xvfb-run -a pnpm e2e:mobile`）で、実画面に出さない。
- `pkill`/`killall` 禁止。残留プロセスは正確な PID のみ kill。

---

### Task 1: セーフエリアの CSS 変数化（本番コード）

**Files:**
- Modify: `frontend/src/app.css`（`:root` に変数追加）
- Modify: `frontend/src/App.svelte:158,170,232,242`
- Modify: `frontend/src/ui/ConfirmDialog.svelte:38`
- Modify: `frontend/src/ui/Modal.svelte:35`
- Modify: `frontend/src/ui/Backstage.svelte:130`
- Modify: `frontend/src/ui/Column.svelte:635`
- Modify: `frontend/src/render/MediaViewer.svelte:463,639`
- Modify: `frontend/src/ui/AppMenu.svelte:40,46`
- Modify: `docs/design/style-guide.md`（規約追記）

**Interfaces:**
- Produces: CSS 変数 `--safe-top` / `--safe-right` / `--safe-bottom` / `--safe-left`（既定は `env(safe-area-inset-*, 0px)`）。Task 2 以降がこれを `documentElement.style.setProperty` で上書きする。

- [ ] **Step 1: `:root` に変数を定義する**

`frontend/src/app.css` の既存 `:root { ... }` ブロック内（先頭のカスタムプロパティ群の近く）に追記:

```css
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-right: env(safe-area-inset-right, 0px);
  --safe-bottom: env(safe-area-inset-bottom, 0px);
  --safe-left: env(safe-area-inset-left, 0px);
```

- [ ] **Step 2: 全ての `env(safe-area-inset-*)` を置換する**

対象は `grep -rn "env(safe-area" frontend/src` の全ヒット（上記 Files の行）。置換規則:

| 旧 | 新 |
|---|---|
| `env(safe-area-inset-top)` | `var(--safe-top)` |
| `env(safe-area-inset-right)` | `var(--safe-right)` |
| `env(safe-area-inset-bottom)` | `var(--safe-bottom)` |
| `env(safe-area-inset-left)` | `var(--safe-left)` |

Tailwind の arbitrary value 内でも `max(6px,var(--safe-top))` や `calc(20px+var(--safe-right))` は有効。`app.css` の定義行自体（`:root` 内の `env()`）は置換しない。AppMenu.svelte:40 はコメント内の記述なので、コメントも新表記へ揃える。

- [ ] **Step 3: 置換漏れが無いことを確認する**

Run: `grep -rn "env(safe-area" frontend/src | grep -v "app.css"`
Expected: 出力なし

- [ ] **Step 4: 型チェックとユニットテスト**

Run: `cd frontend && pnpm check && pnpm test`
Expected: どちらも PASS

- [ ] **Step 5: 規約を style-guide に追記する**

`docs/design/style-guide.md` に節を追加:

```markdown
## セーフエリア

`env(safe-area-inset-*)` を直接書かず、`app.css` の `--safe-top/right/bottom/left` を `var()` で参照する。
E2E（`e2e/specs-mobile/`）がこれらの変数を上書きしてセーフエリア被りを検証するため。
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src docs/design/style-guide.md
git commit -m "refactor: セーフエリアのenv()をCSS変数経由にしE2Eから上書き可能にする"
```

---

### Task 2: モバイルE2E基盤（helper・config・script）

**Files:**
- Create: `e2e/helpers/mobile.ts`
- Create: `e2e/wdio.mobile.conf.ts`
- Modify: `e2e/package.json`（`e2e:mobile` script）
- Modify: `e2e/tsconfig.json`（include に追加）

**Interfaces:**
- Consumes: Task 1 の `--safe-*` 変数。
- Produces（`e2e/helpers/mobile.ts`）:
  - `export const MOBILE_VIEWPORT = { width: 390, height: 844 }`
  - `export const SAFE_AREA = { top: 47, right: 0, bottom: 34, left: 0 }`（px。ノッチ/ジェスチャーバー相当）
  - `export async function addAccountAndEnableMobile(bridge: MiauthBridge, logTag: string): Promise<void>` — MiAuth でアカウント追加 → 設定→レイアウト→「モバイル版」→保存 → ビューポート縮小 → safe-area 注入。
  - `export async function setSafeArea(insets: {top:number;right:number;bottom:number;left:number}): Promise<void>` — `--safe-*` を `documentElement.style` に設定。
  - `export async function rect(selector: string): Promise<{top:number;right:number;bottom:number;left:number;width:number;height:number} | null>` — `browser.execute` で `getBoundingClientRect()`。

- [ ] **Step 1: wdio 専用 config を作る**

`e2e/wdio.mobile.conf.ts`:

```ts
import { config as base } from "./wdio.conf";

// モバイルUI検証専用(Issue #259)。デスクトップspecsとはウィンドウサイズ前提が
// 異なるため、specs-mobile/ を別configで実行する。
export const config = {
  ...base,
  specs: ["./specs-mobile/**/*.e2e.ts"],
};
```

- [ ] **Step 2: script と tsconfig を更新する**

`e2e/package.json` の scripts に追加:

```json
"e2e:mobile": "NODE_EXTRA_CA_CERTS=./certs/ca.pem wdio run wdio.mobile.conf.ts"
```

`e2e/tsconfig.json` の `include` に `"wdio.mobile.conf.ts"`, `"specs-mobile/**/*.ts"` を追加。

- [ ] **Step 3: helper を実装する**

`e2e/helpers/mobile.ts`（アカウント追加の並行呼び出しは `specs/account-post-reaction.e2e.ts` の `Promise.all` パターンを踏襲。コメントの理由もそこに書かれている）:

```ts
import type { MiauthBridge } from "./miauthBridge";
import { clickThroughAccountSelect } from "./accountSelect";

const MISSKEY_HOST = "misskey.local:8443";

export const MOBILE_VIEWPORT = { width: 390, height: 844 } as const;
export const SAFE_AREA = { top: 47, right: 0, bottom: 34, left: 0 } as const;

type Insets = { top: number; right: number; bottom: number; left: number };
export type Rect = { top: number; right: number; bottom: number; left: number; width: number; height: number };

export async function setSafeArea(insets: Insets): Promise<void> {
  await browser.execute((i) => {
    const s = document.documentElement.style;
    s.setProperty("--safe-top", `${i.top}px`);
    s.setProperty("--safe-right", `${i.right}px`);
    s.setProperty("--safe-bottom", `${i.bottom}px`);
    s.setProperty("--safe-left", `${i.left}px`);
  }, insets);
}

export async function rect(selector: string): Promise<Rect | null> {
  return browser.execute((sel) => {
    const el = document.querySelector(sel);
    if (!el) return null;
    const r = el.getBoundingClientRect();
    return { top: r.top, right: r.right, bottom: r.bottom, left: r.left, width: r.width, height: r.height };
  }, selector);
}

export async function addAccountAndEnableMobile(bridge: MiauthBridge, logTag: string): Promise<void> {
  const hostInput = await $('[data-testid="add-account-host-input"]');
  await hostInput.waitForDisplayed({ timeout: 15000 });
  await hostInput.setValue(MISSKEY_HOST);
  const startButton = await $('[data-testid="add-account-start"]');
  await Promise.all([
    bridge.approveNext(),
    clickThroughAccountSelect(bridge.cdpPort, { logTag, accountLabel: "e2etestadmin" }),
    startButton.click(),
  ]);
  const complete = await $('[data-testid="add-account-complete"]');
  await complete.waitForDisplayed({ timeout: 15000 });
  await complete.click();

  // 設定→レイアウト→UIモード「モバイル版」→保存。
  const menuTrigger = await $('[data-testid="app-menu-trigger"]');
  await menuTrigger.waitForDisplayed({ timeout: 15000 });
  await menuTrigger.click();
  const openSettings = await $('[data-testid="app-menu-open-settings"]');
  await openSettings.waitForDisplayed({ timeout: 15000 });
  await openSettings.click();
  const layoutTab = await $('[data-testid="settings-tab-layout"]');
  await layoutTab.waitForDisplayed({ timeout: 15000 });
  await layoutTab.click();
  const mobileBtn = await $("button=モバイル版");
  await mobileBtn.waitForClickable({ timeout: 15000 });
  await mobileBtn.click();
  const saveBtn = await $("button=保存");
  await saveBtn.scrollIntoView();
  await saveBtn.click();
  await $("span=保存しました").waitForDisplayed({ timeout: 15000 });
  // 設定モーダルを閉じる(Modal.svelteの閉じるボタン)。開いたままだとFABが隠れる。
  await $('[data-testid="modal-close"]').click();

  await browser.setWindowSize(MOBILE_VIEWPORT.width, MOBILE_VIEWPORT.height);
  await browser.waitUntil(async () => (await browser.execute(() => window.innerWidth)) <= MOBILE_VIEWPORT.width + 20, {
    timeout: 10000,
    timeoutMsg: "window did not shrink to mobile viewport",
  });
  await setSafeArea(SAFE_AREA);
}
```

- [ ] **Step 4: 前提の実在を確認する**

Run: `grep -n "modal-close" frontend/src/ui/Modal.svelte && grep -n '"保存"' frontend/src/ui/settings/LayoutSection.svelte`
Expected: 両方ヒット（保存ボタンのラベルは「保存」、保存後に「保存しました」が出る、`modal-close` が設定モーダルの閉じるボタン）

- [ ] **Step 5: 型チェック**

Run: `cd e2e && pnpm exec tsc --noEmit -p tsconfig.json`
Expected: エラーなし

- [ ] **Step 6: Commit**

```bash
git add e2e/helpers/mobile.ts e2e/wdio.mobile.conf.ts e2e/package.json e2e/tsconfig.json
git commit -m "test: モバイルE2Eの基盤(helper・config・script)を追加する"
```

---

### Task 3: layout.e2e.ts（FAB・投稿欄・横スナップ）

**Files:**
- Create: `e2e/specs-mobile/layout.e2e.ts`

**Interfaces:**
- Consumes: Task 2 の `addAccountAndEnableMobile`, `rect`, `MOBILE_VIEWPORT`。

- [ ] **Step 1: spec を書く**

```ts
import { startMiauthBridge, type MiauthBridge } from "../helpers/miauthBridge";
import { addAccountAndEnableMobile, rect, MOBILE_VIEWPORT } from "../helpers/mobile";

describe("mobile layout", () => {
  let bridge: MiauthBridge;

  before(async function () {
    this.timeout(120000);
    bridge = await startMiauthBridge();
    await addAccountAndEnableMobile(bridge, "mobileLayout");
  });

  after(async () => {
    await bridge.teardown();
  });

  it("shows the compose FAB and hides the always-on compose bar", async () => {
    const fab = await $('button[title="投稿"]');
    await fab.waitForDisplayed({ timeout: 15000 });
    expect(await $('[data-testid="compose-textarea"]').isDisplayed()).toBe(false);
  });

  it("opens compose as a modal when the FAB is tapped", async () => {
    await $('button[title="投稿"]').click();
    const textarea = await $('[data-testid="compose-textarea"]');
    await textarea.waitForDisplayed({ timeout: 15000 });
    await browser.keys("Escape");
  });

  it("keeps a column at full viewport width", async () => {
    // アカウント追加直後はカラムが無いため、Homeカラムを追加する。
    await $('[data-testid="app-menu-trigger"]').click();
    await $('[data-testid="app-menu-add-column"]').click();
    const submit = await $('[data-testid="add-column-submit"]');
    await submit.scrollIntoView();
    await submit.waitForClickable({ timeout: 15000 });
    await submit.click();

    await $(".column-root").waitForDisplayed({ timeout: 15000 });
    const col = await rect(".column-root");
    expect(col).not.toBeNull();
    expect(Math.round(col!.width)).toBeGreaterThanOrEqual(MOBILE_VIEWPORT.width - 2);
  });
});
```

- [ ] **Step 2: セレクタの実在を確認する**

Run: `grep -n 'column-root' frontend/src/ui/Column.svelte`
Expected: `<section class="column-root ...">` がヒット（カラム幅は `useMobileUi()` 時に `flex:0 0 100%` になる）

- [ ] **Step 3: 実行して PASS を確認する**

Run: `cd e2e && xvfb-run -a pnpm e2e:mobile --spec ./specs-mobile/layout.e2e.ts`
（事前に `docker compose up -d --wait` と `pnpm seed`、`cd src-tauri && cargo build` 済みであること。手順は `e2e/README.md`）
Expected: 3 passing

- [ ] **Step 4: Commit**

```bash
git add e2e/specs-mobile/layout.e2e.ts
git commit -m "test: モバイルレイアウト(FAB・投稿モーダル・カラム幅)のE2Eを追加する"
```

---

### Task 4: safe-area.e2e.ts（セーフエリア被り）

**Files:**
- Create: `e2e/specs-mobile/safe-area.e2e.ts`

**Interfaces:**
- Consumes: `SAFE_AREA`, `rect`, `addAccountAndEnableMobile`。

- [ ] **Step 1: spec を書く**

```ts
import { startMiauthBridge, type MiauthBridge } from "../helpers/miauthBridge";
import { addAccountAndEnableMobile, rect, SAFE_AREA } from "../helpers/mobile";

describe("mobile safe-area", () => {
  let bridge: MiauthBridge;

  before(async function () {
    this.timeout(120000);
    bridge = await startMiauthBridge();
    await addAccountAndEnableMobile(bridge, "mobileSafeArea");
  });

  after(async () => {
    await bridge.teardown();
  });

  it("keeps the compose FAB inside the bottom/right safe area", async () => {
    const fab = await rect('button[title="投稿"]');
    expect(fab).not.toBeNull();
    const vh = await browser.execute(() => window.innerHeight);
    const vw = await browser.execute(() => window.innerWidth);
    expect(fab!.bottom).toBeLessThanOrEqual(vh - SAFE_AREA.bottom);
    expect(fab!.right).toBeLessThanOrEqual(vw - SAFE_AREA.right);
  });

  it("keeps the compose modal content below the top safe area", async () => {
    await $('button[title="投稿"]').click();
    const textarea = await $('[data-testid="compose-textarea"]');
    await textarea.waitForDisplayed({ timeout: 15000 });
    const r = await rect('[data-testid="compose-textarea"]');
    expect(r!.top).toBeGreaterThanOrEqual(SAFE_AREA.top);
    await browser.keys("Escape");
  });

  it("keeps the bottom menu bar above the bottom safe area padding", async () => {
    // AppMenu/Backstage の下端パディングが --safe-bottom 分確保されているか。
    const trigger = await rect('[data-testid="app-menu-trigger"]');
    const vh = await browser.execute(() => window.innerHeight);
    expect(trigger!.bottom).toBeLessThanOrEqual(vh - SAFE_AREA.bottom);
  });
});
```

- [ ] **Step 2: メディアビューワーの検証を追加する**

`data-testid="media-page"` は既存。メディア付きノートの用意には `e2e/helpers/misskeyApi.ts` の投稿ヘルパー（画像アップロード可否を `grep -n "export" e2e/helpers/misskeyApi.ts` で確認）を使う。アップロードヘルパーが無い場合はこのステップを別Issueに切り出し（メディアビューワーの検証はスコープ縮小）、spec に「メディアビューワーは未カバー」と明記する。ヘルパーがある場合は、ビューワーを開き、上部の閉じるボタン行の `top >= SAFE_AREA.top`、下部操作行の `bottom <= vh - SAFE_AREA.bottom` を検証する。

- [ ] **Step 3: 実行して PASS を確認する**

Run: `cd e2e && xvfb-run -a pnpm e2e:mobile --spec ./specs-mobile/safe-area.e2e.ts`
Expected: PASS

- [ ] **Step 4: 検出力を確認する（設計書 4節）**

一時的に `frontend/src/App.svelte:232` の `bottom-[calc(20px+var(--safe-bottom))]` を `bottom-[20px]` に戻し、同コマンドを再実行して 1つ目のテストが **FAIL** することを確認する。確認後、必ず元に戻す（`git checkout frontend/src/App.svelte`）。

- [ ] **Step 5: Commit**

```bash
git add e2e/specs-mobile/safe-area.e2e.ts
git commit -m "test: モバイルのセーフエリア被りを検出するE2Eを追加する"
```

---

### Task 5: overflow.e2e.ts（横はみ出し）

**Files:**
- Create: `e2e/specs-mobile/overflow.e2e.ts`

- [ ] **Step 1: spec を書く**

```ts
import { startMiauthBridge, type MiauthBridge } from "../helpers/miauthBridge";
import { addAccountAndEnableMobile } from "../helpers/mobile";

describe("mobile horizontal overflow", () => {
  let bridge: MiauthBridge;

  before(async function () {
    this.timeout(120000);
    bridge = await startMiauthBridge();
    await addAccountAndEnableMobile(bridge, "mobileOverflow");
  });

  after(async () => {
    await bridge.teardown();
  });

  it("has no page-level horizontal overflow", async () => {
    const over = await browser.execute(() => document.documentElement.scrollWidth - document.documentElement.clientWidth);
    expect(over).toBeLessThanOrEqual(0);
  });

  it("has no horizontal overflow while the compose modal is open", async () => {
    await $('button[title="投稿"]').click();
    await $('[data-testid="compose-textarea"]').waitForDisplayed({ timeout: 15000 });
    const over = await browser.execute(() => document.documentElement.scrollWidth - document.documentElement.clientWidth);
    expect(over).toBeLessThanOrEqual(0);
    await browser.keys("Escape");
  });
});
```

カラム列の横スクロールは意図した挙動（scroll-snap）なので、検証対象は `documentElement` のみとし、カラム列コンテナは対象外とする。

- [ ] **Step 2: 実行して PASS を確認する**

Run: `cd e2e && xvfb-run -a pnpm e2e:mobile --spec ./specs-mobile/overflow.e2e.ts`
Expected: 2 passing

- [ ] **Step 3: Commit**

```bash
git add e2e/specs-mobile/overflow.e2e.ts
git commit -m "test: モバイルの意図しない横はみ出しを検出するE2Eを追加する"
```

---

### Task 6: 全体回帰確認・README・CI 判断

**Files:**
- Modify: `e2e/README.md`（`pnpm e2e:mobile` の説明追記）
- Modify（条件付き）: `.github/workflows/test.yml`

- [ ] **Step 1: 全 specs を通しで実行する**

Run: `cd e2e && xvfb-run -a pnpm e2e:mobile` および `xvfb-run -a pnpm e2e`
Expected: どちらも全 PASS（デスクトップ E2E の回帰が無いこと）。所要時間を記録する。

- [ ] **Step 2: README に追記する**

`e2e/README.md` に「モバイルUI E2E（`pnpm e2e:mobile`）」節を追加: 目的、実行方法（`xvfb-run -a pnpm e2e:mobile`）、限界（Android WebView 固有挙動は対象外、実機確認は手動）。

- [ ] **Step 2b: 残留プロセスを確認する**

Run: `ps aux | grep -E "Xvfb|gnome-keyring|tsumugi" | grep -v grep`
残留があれば PID を指定して kill する（名前パターンでの一括 kill は禁止）。

- [ ] **Step 3: CI 追加の要否を判断する**

Step 1 の所要時間が既存 e2e ジョブの許容内（`timeout-minutes: 45`）に収まるなら、`test.yml` の `Run E2E tests` の直後に同一ジョブ内のステップとして追加する:

```yaml
      - name: Run mobile E2E tests
        if: needs.changes.outputs.code == 'true'
        working-directory: e2e
        run: xvfb-run -a pnpm e2e:mobile
```

収まらない、または不安定な場合は追加せず README にその旨を記録する。判断結果をユーザーに報告する。

- [ ] **Step 4: Commit**

```bash
git add e2e/README.md .github/workflows/test.yml
git commit -m "docs: モバイルE2Eの実行方法を追記する"
```

- [ ] **Step 5: PR を作成する**

`Fixes #259` を本文に含め、`.github/pull_request_template.md` の構成に手動で従う。マージは `gh pr merge --merge`。push 後に CI を Monitor で待たない。
